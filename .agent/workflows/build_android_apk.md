---
description: Build the Android APK for the application
---

1. Run the flutter build command to generate the APK:
// turbo
```bash
flutter build apk --release
```

2. The APK will be generated at `build/app/outputs/flutter-apk/app-release.apk`.
